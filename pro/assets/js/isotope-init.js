jQuery(function ($) {
    $('.main_openagenda').isotope({
        // options
        itemSelector: '.openagenda_masonry',
        layoutMode: 'masonry',
    });
})


jQuery(function ($) {
    $('.main_openagenda').masonry({
        itemSelector: '.openagenda_masonry',
    });
})

